import { expect, describe, it, beforeAll, afterAll } from '@jest/globals';
import mongoose from 'mongoose';

describe("authentication-guarded API", () => {
    it("should be true", async () => {
        expect(null).toBe(null);
    });
});
